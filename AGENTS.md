# Hướng dẫn dành cho AI khi làm việc với repository này

Repository này được thiết kế cho người không chuyên. Khi hỗ trợ người dùng:

1. Trình bày bằng tiếng Việt đơn giản, tránh thuật ngữ kỹ thuật nếu không thật sự cần thiết.
2. Tự thực hiện thao tác cài đặt, chạy thử và render. Không yêu cầu người dùng gõ lệnh trong Terminal.
3. Đọc `README.md`, `QUY-TRINH.md`, `ASSETS.md` và prompt liên quan trước khi sửa video.
4. Dự án Remotion nằm trong thư mục `project`. Composition chính là `ReelVI`; bản tiếng Anh là `Reel`.
5. Không sửa hoặc xóa asset gốc. Khi cần thay asset, thêm file mới với tên rõ ràng rồi cập nhật mã nguồn.
6. Giữ nguyên các scene không nằm trong yêu cầu của người dùng.
7. Sau mỗi thay đổi quan trọng, kiểm tra composition, render một ảnh đại diện và cho người dùng xem trước.
8. Chỉ render video hoàn chỉnh sau khi hình ảnh, nội dung, caption và âm thanh đã được duyệt.
9. Nếu thời lượng audio thay đổi, phải căn lại thời lượng composition, caption và thời điểm xuất hiện của chữ.
10. Báo rõ file nào đã thay đổi và video được xuất ở đâu.

Nếu môi trường có skill Remotion, hãy sử dụng skill đó để tuân thủ các thực hành tốt nhất.
